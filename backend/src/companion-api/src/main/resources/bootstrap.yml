spring:
  application:
    name: companion-api

  # 本地开发禁用 NACOS，避免无 NACOS 时启动失败
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
