spring:
  application:
    name: companion-api

  # 通过环境变量 SPRING_PROFILES_ACTIVE 切换环境，默认 dev
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # NACOS 默认关闭；如需启用，在对应环境变量或配置中覆盖
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
