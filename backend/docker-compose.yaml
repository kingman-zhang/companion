version: '3.2'
networks:
  app-net:
    driver: bridge
services:
  app:
    build: .
    container_name: companion-app
    restart: always
    environment:
      SPRING_PROFILES_ACTIVE: prod
      MONGODB_URI: mongodb://root:hpHhEHmtt7v*@mongo:27017/companion?authSource=admin
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: vmQvyBHUKEgx
      JWT_SECRET: companion-prod-secret-key-must-be-at-least-32-chars-long
      LOG_PATH: /app/logs
    volumes:
      - /data/logs/companion:/app/logs
    depends_on:
      - mongo
      - redis
    networks:
      - app-net
