/* P4 消息改写页 — 玫瑰粉色系 */

.page {
  height: 100vh;
  background:
    radial-gradient(circle at top, rgba(255, 255, 255, 0.92) 0%, rgba(255, 255, 255, 0) 36%),
    linear-gradient(180deg, #FFF8F5 0%, #FAF1ED 48%, #F6EEEA 100%);
  display: flex;
  flex-direction: column;
}

/* ══════════════════════════
   导航栏
══════════════════════════ */
.nav-bar {
  padding-left: 32rpx;
  padding-right: 32rpx;
  padding-bottom: 16rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-shrink: 0;
  background: rgba(250, 241, 237, 0.92);
  border-bottom: 1rpx solid rgba(228, 210, 205, 0.84);
  backdrop-filter: blur(18rpx);
}

.nav-back-btn {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.92);
  border: 1rpx solid rgba(219, 200, 195, 0.72);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 10rpx 24rpx rgba(117, 77, 76, 0.06);
}

.nav-btn-hover { opacity: 0.7; }

.nav-back-icon {
  font-size: 44rpx;
  color: #52464A;
  line-height: 1;
  margin-top: -4rpx;
}

.nav-center {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.nav-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #1F1417;
  line-height: 1.3;
}

.nav-subtitle {
  font-size: 20rpx;
  color: #8F8589;
  line-height: 1;
}

.nav-right {
  min-width: 60rpx;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 20rpx;
}

.nav-action-text {
  font-size: 24rpx;
  color: #8F8589;
}

.nav-history-text {
  color: #E45A7E;
}

/* ══════════════════════════
   可滚动内容区
══════════════════════════ */
.content {
  flex: 1;
  overflow: hidden;
}

/* ══════════════════════════
   输入状态：Hero
══════════════════════════ */
.hero {
  padding: 32rpx 40rpx 24rpx;
  text-align: center;
}

.hero-title {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
  color: #1F1417;
  line-height: 1.4;
  letter-spacing: -0.5rpx;
  margin-bottom: 12rpx;
}

.hero-desc {
  display: block;
  font-size: 26rpx;
  color: #8F8589;
  line-height: 1.65;
}

/* ── Step 块 ── */
.step-block {
  padding: 0 40rpx 32rpx;
}

.step-label-row {
  display: flex;
  align-items: center;
  gap: 14rpx;
  margin-bottom: 20rpx;
}

.step-num {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.step-num-on  { background: #E45A7E; }
.step-num-off { background: #EFE4E0; }

.step-num-text {
  font-size: 20rpx;
  font-weight: 700;
  color: #FFFFFF;
  line-height: 1;
}

.step-label-text {
  font-size: 24rpx;
  font-weight: 500;
  color: #52464A;
}

/* ── 目标 chips ── */
.goal-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 12rpx;
}

.goal-chip {
  padding: 14rpx 24rpx;
  border-radius: 18rpx;
  display: flex;
  align-items: center;
}

.goal-chip-on {
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  border: none;
  box-shadow: 0 10rpx 22rpx rgba(199, 62, 100, 0.16);
}

.goal-chip-off {
  background: rgba(255, 255, 255, 0.92);
  border: 1rpx solid rgba(228, 210, 205, 0.84);
}

.chip-hover { opacity: 0.75; }

.goal-chip-text {
  font-size: 26rpx;
  font-weight: 500;
  color: #52464A;
}

.goal-chip-text-on {
  color: #FFFFFF;
}

.goal-sub {
  display: block;
  font-size: 22rpx;
  color: #8F8589;
  line-height: 1.5;
  margin-top: 4rpx;
}

/* ── 输入卡片 ── */
.input-card {
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.94);
  border: 2rpx solid rgba(228, 210, 205, 0.84);
  overflow: hidden;
  transition: border-color 0.2s;
  box-shadow: 0 18rpx 42rpx rgba(117, 77, 76, 0.06);
}

.input-card-active {
  border-color: rgba(228, 90, 126, 0.36);
}

.original-textarea {
  display: block;
  width: 100%;
  min-height: 180rpx;
  padding: 28rpx 28rpx 16rpx;
  font-size: 30rpx;
  color: #1F1417;
  line-height: 1.7;
  box-sizing: border-box;
}

.textarea-placeholder {
  color: #BDB3B5;
  font-size: 30rpx;
}

/* 输入卡片底部：风险 + 字数 */
.input-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14rpx 28rpx 20rpx;
  border-top: 1rpx solid rgba(228, 210, 205, 0.84);
}

/* ── 风险提示（共用）── */
.risk-hint {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.risk-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
}

.risk-dot-low    { background: #3D7A5A; }
.risk-dot-medium { background: #A07830; }
.risk-dot-high   { background: #9B4040; }

.risk-label-text {
  font-size: 22rpx;
  font-weight: 500;
}

.risk-label-low    { color: #3D7A5A; }
.risk-label-medium { color: #A07830; }
.risk-label-high   { color: #9B4040; }

.char-count {
  font-size: 22rpx;
  color: #BDB3B5;
}

/* ── 示例 ── */
.examples {
  margin-top: 20rpx;
}

.examples-title {
  display: block;
  font-size: 22rpx;
  color: #8F8589;
  margin-bottom: 14rpx;
}

.example-item {
  padding: 20rpx 24rpx;
  border-radius: 20rpx;
  background: rgba(255, 255, 255, 0.88);
  border: 1.5rpx dashed rgba(228, 210, 205, 0.84);
  margin-bottom: 12rpx;
}

.example-text {
  font-size: 26rpx;
  color: #52464A;
  line-height: 1.6;
}

/* ── 生成按钮（固定底部）── */
.gen-bar {
  padding: 16rpx 40rpx calc(env(safe-area-inset-bottom, 0px) + 24rpx);
  background: rgba(250, 241, 237, 0.94);
  border-top: 1rpx solid rgba(228, 210, 205, 0.84);
  flex-shrink: 0;
  backdrop-filter: blur(18rpx);
}

.gen-btn {
  height: 96rpx;
  border-radius: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.gen-btn-on  {
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  box-shadow: 0 16rpx 30rpx rgba(199, 62, 100, 0.18);
}
.gen-btn-off { background: #EFE4E0; }

.btn-hover { opacity: 0.82; }

.gen-btn-text {
  font-size: 30rpx;
  font-weight: 600;
  color: #FFFFFF;
}

.gen-btn-off .gen-btn-text { color: #BDB3B5; }

/* ══════════════════════════
   结果状态
══════════════════════════ */

/* 原文回显卡 */
.echo-card {
  margin: 24rpx 40rpx 0;
  padding: 24rpx 28rpx;
  background: rgba(255, 255, 255, 0.94);
  border: 1rpx solid rgba(228, 210, 205, 0.84);
  border-radius: 28rpx;
  box-shadow: 0 16rpx 36rpx rgba(117, 77, 76, 0.06);
}

.echo-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12rpx;
}

.echo-label {
  font-size: 22rpx;
  color: #8F8589;
}

.echo-text {
  display: block;
  font-size: 26rpx;
  color: #52464A;
  line-height: 1.7;
}

/* ── Tab 切换 ── */
.tab-switcher {
  margin: 24rpx 40rpx 0;
  display: flex;
  gap: 8rpx;
  background: rgba(255, 248, 245, 0.94);
  border-radius: 24rpx;
  padding: 8rpx;
  border: 1rpx solid rgba(228, 210, 205, 0.84);
}

.tab-item {
  flex: 1;
  padding: 16rpx 8rpx;
  border-radius: 18rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

.tab-on {
  background: rgba(255, 255, 255, 0.96);
  box-shadow: 0 10rpx 22rpx rgba(117, 77, 76, 0.06);
}

.tab-off { background: transparent; }

.tab-label {
  font-size: 26rpx;
  font-weight: 500;
  color: #8F8589;
  line-height: 1.3;
}

.tab-label-on {
  color: #1F1417;
  font-weight: 600;
}

.tab-sub {
  font-size: 20rpx;
  line-height: 1;
}

.tab-sub-low    { color: #3D7A5A; }
.tab-sub-medium { color: #A07830; }
.tab-sub-high   { color: #9B4040; }

/* ── 结果卡 ── */
.result-card {
  margin: 20rpx 40rpx 0;
  padding: 32rpx;
  background: rgba(255, 255, 255, 0.95);
  border: 1rpx solid rgba(228, 210, 205, 0.84);
  border-radius: 28rpx;
  min-height: 240rpx;
  box-shadow: 0 18rpx 40rpx rgba(117, 77, 76, 0.06);
}

.result-text {
  display: block;
  font-size: 30rpx;
  color: #1F1417;
  line-height: 1.8;
  margin-bottom: 28rpx;
}

/* 操作行 */
.result-actions {
  display: flex;
  gap: 12rpx;
}

.action-copy {
  flex: 1;
  height: 72rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 12rpx 24rpx rgba(199, 62, 100, 0.16);
}

.action-copy-done { background: #3D7A5A; }

.action-copy-text {
  font-size: 26rpx;
  font-weight: 500;
  color: #FFFFFF;
}

.action-icon-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: 18rpx;
  border: 1rpx solid rgba(228, 210, 205, 0.84);
  background: rgba(255, 255, 255, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-icon {
  font-size: 32rpx;
  color: #52464A;
}

/* ── 锁定态：毛玻璃遮罩 ── */
.locked-container {
  position: relative;
  min-height: 360rpx;
  overflow: hidden;
  border-radius: 20rpx;
}

.result-text-blurred {
  filter: blur(6px);
  opacity: 0.5;
  user-select: none;
  pointer-events: none;
}

.lock-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(250, 244, 241, 0.88);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 32rpx;
  border-radius: 20rpx;
  backdrop-filter: blur(14rpx);
}

.lock-badge {
  width: 72rpx;
  height: 72rpx;
  background: rgba(255, 255, 255, 0.96);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10rpx 24rpx rgba(117, 77, 76, 0.08);
  margin-bottom: 4rpx;
}

.lock-badge-icon {
  font-size: 32rpx;
  line-height: 1;
}

.lock-overlay-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #1F1417;
  text-align: center;
}

.lock-overlay-sub {
  font-size: 22rpx;
  color: #8F8589;
  text-align: center;
  line-height: 1.5;
}

.lock-overlay-btn {
  margin-top: 8rpx;
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  border-radius: 999rpx;
  padding: 16rpx 48rpx;
  box-shadow: 0 12rpx 24rpx rgba(199, 62, 100, 0.16);
}

.lock-overlay-btn-text {
  font-size: 26rpx;
  font-weight: 600;
  color: #FFFFFF;
}

/* ── 预测 Nudge ── */
.nudge-banner {
  margin: 24rpx 40rpx 0;
  padding: 24rpx;
  border-radius: 28rpx;
  background: linear-gradient(180deg, #FFF4F7 0%, #FCE8EE 100%);
  display: flex;
  align-items: center;
  gap: 16rpx;
  border: 1rpx solid rgba(228, 90, 126, 0.14);
  box-shadow: 0 16rpx 36rpx rgba(199, 62, 100, 0.08);
}

.nudge-icon-wrap {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.nudge-icon {
  font-size: 24rpx;
  color: #FFFFFF;
}

.nudge-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.nudge-title {
  display: block;
  font-size: 26rpx;
  font-weight: 600;
  color: #C73E64;
  line-height: 1.3;
}

.nudge-sub {
  display: block;
  font-size: 20rpx;
  color: #E45A7E;
  line-height: 1.4;
}

.nudge-btn {
  padding: 12rpx 24rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  flex-shrink: 0;
  box-shadow: 0 10rpx 20rpx rgba(199, 62, 100, 0.14);
}

.nudge-btn-text {
  font-size: 22rpx;
  font-weight: 500;
  color: #FFFFFF;
}

/* ══════════════════════════
   历史记录
══════════════════════════ */
.hist-fallback-tip {
  margin: 20rpx 40rpx 0;
  padding: 14rpx 24rpx;
  background: rgba(253, 246, 227, 0.92);
  border-radius: 18rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border: 1rpx solid rgba(160, 120, 48, 0.14);
}

.hist-fallback-text {
  font-size: 22rpx;
  color: #A07830;
}

.hist-fallback-retry {
  font-size: 22rpx;
  color: #C73E64;
  font-weight: 600;
}

.hist-empty {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}

.hist-empty-text {
  font-size: 28rpx;
  color: #BDB3B5;
}

.hist-item {
  margin: 20rpx 40rpx 0;
  padding: 24rpx 28rpx;
  background: rgba(255, 255, 255, 0.94);
  border-radius: 24rpx;
  border: 1rpx solid rgba(228, 210, 205, 0.84);
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  box-shadow: 0 14rpx 34rpx rgba(117, 77, 76, 0.05);
}

.hist-item-hover { opacity: 0.9; }

.hist-item-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16rpx;
}

.hist-original {
  flex: 1;
  font-size: 28rpx;
  font-weight: 500;
  color: #1F1417;
  line-height: 1.5;
}

.hist-date {
  font-size: 22rpx;
  color: #BDB3B5;
  flex-shrink: 0;
  padding-top: 4rpx;
}

.hist-result {
  display: block;
  font-size: 24rpx;
  color: #8F8589;
  line-height: 1.6;
}

/* ── 升级引导弹窗 ── */
.upgrade-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

.upgrade-modal {
  width: 600rpx;
  background: rgba(255, 255, 255, 0.98);
  border-radius: 32rpx;
  padding: 48rpx 40rpx 40rpx;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
  box-shadow: 0 24rpx 60rpx rgba(65, 33, 38, 0.16);
}

.upgrade-modal-close {
  position: absolute;
  top: 20rpx;
  right: 24rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.upgrade-modal-close-icon {
  font-size: 44rpx;
  color: #C0BAB0;
  line-height: 1;
  font-weight: 300;
}

.upgrade-modal-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1C1C1E;
  text-align: center;
}

.upgrade-modal-body {
  display: block;
  width: 100%;
  font-size: 26rpx;
  color: #666;
  text-align: center;
  line-height: 1.7;
  white-space: normal;
  word-break: break-word;
  box-sizing: border-box;
}

.upgrade-modal-email-wrap {
  background: #FCF4F1;
  border-radius: 16rpx;
  padding: 20rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  width: 100%;
  box-sizing: border-box;
}

.upgrade-modal-email {
  font-size: 28rpx;
  font-weight: 600;
  color: #C73E64;
  letter-spacing: 0.5rpx;
}

.upgrade-modal-copy-hint {
  font-size: 20rpx;
  color: #A0A0A0;
}

.email-hover {
  opacity: 0.7;
}

.upgrade-modal-btn {
  margin-top: 8rpx;
  width: 100%;
  background: linear-gradient(135deg, #E45A7E 0%, #C73E64 100%);
  border-radius: 999rpx;
  padding: 28rpx 0;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 14rpx 28rpx rgba(199, 62, 100, 0.16);
}

.upgrade-modal-btn-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #FFFFFF;
}
